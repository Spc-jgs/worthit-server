spring:
  application:
    name: worthit-gateway

server:
  port: 18080

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  endpoints:
    web:
      exposure:
        include: health,info
