# WorthIt Server Agent Guide

## 适用范围

本文件适用于整个 `worthit-server` 后端仓库。进入任意子目录工作时，仍须遵守本文件及 `rules/` 中的规则。

## 开始任务前

1. 阅读本文件。
2. 阅读 [后端规则索引](rules/README.md)。
3. 按任务类型阅读 `rules/` 中列出的必读规则。
4. 阅读上级 [WorthIt 项目文档定稿索引](../docs/README.md)，从“当前唯一有效基线”表进入本次任务需要的终稿。
5. 检查仓库根目录、当前分支、Git 状态和已有修改，先确认任务范围再实施。

涉及需求实现、接口契约、数据库或验收时，不得只读规则摘要，必须打开文档索引登记的对应终稿。

涉及 Web API、Swagger/OpenAPI 或 Web 运行时依赖时，必须同时核对
`rules/10-architecture.md`、`rules/20-java-code-style.md`、
`rules/30-spring-maven.md` 和 `rules/40-testing-quality.md`，并执行公网/内部
文档分组及 Servlet/WebFlux 依赖隔离门禁。

项目 Maven 构建统一使用仓库根目录 `./mvnw`，以 Wrapper 固定的 Maven 和校验值
为准；不得用开发机或 CI 预装 Maven 的偶然版本作为通过证据。

## 规则优先级

1. 用户在当前任务中的明确要求。
2. `../docs/README.md` 登记的现行权威文档。
3. 本文件。
4. `rules/` 下的专题规则。
5. 不与上述约束冲突的既有实现习惯。

低优先级内容不得覆盖高优先级约束。发现冲突时，停止受影响的实现，列出冲突位置并请求确认。

## 工程决策与问题治理

- 发现问题后不得默认选择“改动最少”或“先让当前用例通过”的补丁。先定位根因、
  影响范围和约束，再比较有实际可行性的候选方案。
- 方案决策至少同时评估：业务正确性、端到端链路完整性、架构和数据边界、
  性能与资源成本、安全与隐私、可维护性与可测试性、迁移回滚和长期总成本。
  推荐方案必须说明关键证据、被放弃方案及取舍，不以个人偏好代替论证。
- “整体最优”以当前权威文档、阶段范围和真实规模为边界，不等于无限重构、
  堆叠抽象或照搬大厂复杂度。既不能用范围克制为局部补丁辩护，也不能用追求
  完美为无证据的过度设计辩护。
- 同一根因在当前授权范围内影响多个入口、模块或配置时，应一次性闭合所有已确认
  的受影响点，并补齐契约、数据、错误处理、安全、测试、运行配置和交付门禁；
  不得只修复最先暴露的症状，把同类缺陷留在链路其他位置。
- 业务链路按实际调用路径审视：公网入口、Gateway、安全身份、接口契约、
  Application/Domain、事务与数据约束、跨服务调用、失败恢复、可观测性、
  自动化测试和部署配置。当前任务不涉及的环节可以明确标记不适用，但不能静默
  假设已经完成。
- 代码设计优先保持职责和所有权清晰、依赖方向稳定、命名准确、失败语义明确；
  抽象必须来自已经存在的共同语义和变化原因。性能结论需要复杂度、SQL/远程调用
  次数、基准或运行指标等证据；安全设计默认最小权限、可信边界清晰、Secret
  不落库不落日志、外部输入不可信、失败时不放宽保护。
- 如果整体最优解需要跨模块、改变契约、迁移数据、调整架构或扩大阶段范围，先提交
  包含候选方案、影响、迁移、回滚和验证方式的设计并获得用户确认。确认后应完整
  实施已批准方案，不得在实现中无说明地退化成局部补丁。
- 完成标准以“根因消除且授权范围内的链路闭合”为准。单测变绿、单接口可用或
  局部代码更简短都不能单独证明方案已经完成。

## 工作方式

- 业务开发以当前任务涉及的需求、接口和架构终稿为默认输入，目标是完成当前业务闭环，不在实现过程中转做全仓架构治理。
- 需求、架构、接口或数据语义不明确时先提问，不自行补全业务含义。
- 跨模块、改变契约或扩大阶段范围的工作，先提交设计并获得用户确认。
- 现行文档和架构不是不可质疑的教条，但只评估与当前业务任务直接相关的问题；其他问题只记录，不顺手修改。
- 发现当前任务依赖的文档或架构存在问题时，先说明证据、对当前任务的影响、
  候选方案和推荐的整体最优方案；触发架构变更门禁时，必须补充迁移、回滚和
  完整验证设计。
- 架构推翻或重构必须先获得用户确认，再同步修改代码、规则与权威文档；确认前不得以“优化”为由静默偏离现行基线。
- 实现中发现重复代码时，必须判断语义、所有权和变化原因，再决定保留、局部提取或下沉 Common；不得为了消除重复、追求 Common 完整或假设未来复用而提前抽象。
- 未经当前任务授权，不引入新的中间件、扫描工具、CI、制品治理或全仓格式化，也不开展与业务交付无关的基础设施改造。
- 只修改当前任务授权的仓库和文件；不顺带修改上级文档、前端仓库、环境配置或用户已有改动。
- 实施前确认可验证的完成标准；实施后执行与风险相称的测试。
- 结论必须区分已实现、已验证、未验证和后续事项，并给出命令或其他可检查证据。
- 未经明确要求，不执行提交、推送、合并、变基、删分支或其他扩大 Git 状态影响的操作。

## 文档新鲜度

- 每次任务完成前必须执行一次文档新鲜度检查，根据实际变更核对权威文档索引、规则、
  设计、任务状态、运行手册、README、接口、数据库和测试资料；不得只检查代码与测试。
- 当前任务改变需求范围、接口契约、架构或数据边界、运行方式、验收门禁、已完成状态或
  待办结论时，相关且属于当前授权范围的文档必须在同一任务中同步更新后才能宣称完成。
- 进度和待办文档必须直接更新已失效的阶段、状态与结论；不得只在旧结论后追加说明，
  造成同一文件同时存在互相冲突的“当前状态”。历史事实需要保留时必须明确标记日期和
  历史属性。
- 如果应更新的文档位于任务未授权的仓库、存在用户未提交修改，或需要先建立新的权威
  契约版本，停止对该文档的写入，报告精确缺口并取得确认；不得以越权修改或静默跳过
  维持表面上的文档一致。
- 任务结论必须说明本次检查过哪些相关文档、实际更新了什么，以及哪些文档因何未更新。

## 本地中间件环境

本项目本地联调所需的 MySQL、Redis、Nacos 等中间件由独立仓库
`/Users/shaopc/Documents/Script/dev-stack` 提供。

需要使用本地中间件时：

1. 先阅读该仓库根目录 `README.md`，再阅读目标中间件对应的
   `conf/<middleware>/README.md`；以其中当前命令、Profile、端口和初始化说明为准。
2. 启动前检查 `docker compose ps` 和目标容器状态，只启动本次任务需要的最小
   服务集合；不得停止或重建任务开始前已运行且不属于本次任务的服务。
3. `dev-stack` 是独立 Git 仓库。除非用户明确授权，不得修改、提交或推送其中的
   文件；其改动不得混入 `worthit-server` 的提交。
4. 不读取、输出、复制或提交 `dev-stack/.env` 中的密钥。WorthIt 仓库只保存
   非敏感默认值、变量名和模板，密码及 JWT Secret 等必须从环境变量或 Secret
   注入。
5. 禁止执行 `docker compose down -v`、Docker prune、删除数据卷或清空持久化
   数据等破坏性操作，除非用户明确指定目标并授权。
6. 容器为 `Up` 不等于服务可用。联调证据至少包含 Compose 渲染结果、容器
   health/状态、关键日志，以及协议级探测；Nacos 还须确认实际镜像/运行版本与
   当前项目基线 `3.0.3` 一致。
7. 自动化单元测试和常规 Maven 门禁不得依赖开发机残留容器或数据。需要真实
   中间件的测试应使用隔离的 Testcontainers，或放入显式启用的本地集成测试
   Profile。
8. 任务结束时说明由本次任务启动且仍在运行的容器，并给出精确停止命令；不要
   擅自停止用户原先已运行的服务。

## 文档不可用

如果 `../docs/README.md` 或其登记的终稿无法读取，停止依赖该资料的实现并报告缺失文件。不得凭经验发明字段、状态、错误码、接口、表结构、业务流程或 Maven 模块。
